# Backend API (Flask)
Flask>=3.0.0,<4
numpy>=1.26.0,<3

# 阿里云短信服务
alibaba-cloud-sdk-dysmsapi>=1.6.55

# 环境变量加载（.env 文件）
python-dotenv>=1.0.0

# Optional: backend/seed_data.py bulk fake users
Faker>=24.0.0
